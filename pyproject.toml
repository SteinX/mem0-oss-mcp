[project]
name = "mem0-oss-mcp"
version = "0.1.1"
description = "MCP bridge for self-hosted Mem0 OSS server"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
